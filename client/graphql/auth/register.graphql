mutation RegisterUserMutation(
	$name: String!
	$email: String!
	$password: String!
	$loginDirectly: Boolean!
) {
	RegisterUser(
		name: $name
		email: $email
		password: $password
		loginDirectly: $loginDirectly
	) {
		msg
		fields
		success
	}
}
