query PostByIdQuery($postId: String!) {
  PostById(postId: $postId) {
    data {
      id
      title
      description
      createdAt
      updatedAt
      authorId
      author {
        name
      }
    }
    msg
  }
}
